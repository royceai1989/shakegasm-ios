{
  "name": "shakegasm-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor iOS shell for ShakeGasm — bundles the web app, built + submitted to TestFlight via GitHub Actions (macOS). No App Store secrets live in this repo; the App Store Connect key is in encrypted GitHub Secrets.",
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/filesystem": "^6.0.4",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor/preferences": "^6.0.4",
    "@capacitor/share": "^6.0.4",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
